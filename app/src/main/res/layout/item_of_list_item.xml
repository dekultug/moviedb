<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:foreground="?selectableItemBackgroundBorderless"
        android:focusable="true"
        android:clickable="true">

        <TextView
            android:id="@+id/tvItemOfListName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/dimen_16"
            android:padding="@dimen/dimen_10"
            android:textSize="@dimen/dimen_text_size_14"
            android:textColor="@color/black"
            tools:text= "Test"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:fontFamily="@font/svn_regular"/>

        <TextView
            android:id="@+id/tvItemOfListSize"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="(4 items)"
            android:textSize="@dimen/dimen_text_size_14"
            android:fontFamily="@font/svn_regular"
            android:textColor="@color/black"
            app:layout_constraintStart_toEndOf="@id/tvItemOfListName"
            app:layout_constraintTop_toTopOf="@id/tvItemOfListName"
            app:layout_constraintBottom_toBottomOf="@id/tvItemOfListName"/>

        <ImageView
            android:id="@+id/ivItemOfListSelect"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/dimen_12"
            android:src="@drawable/ic_selected"
            android:visibility="gone"
            tools:visibility="visible"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
